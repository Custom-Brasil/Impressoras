to run the example:

1. Run the "copy libraries x64.bat" (if is a 64bit OS) or "copy libraries x86.bat" (if is a 32bit OS) to copy here the libraries
2. Run the "run x64.bat" (if is a 64bit OS) or "run x86.bat" (if is a 32bit OS) to execute the SW